typedef struct {
    i32 x;
} Box;

void consume(Box b) {
    b.x = b.x;
}

i32 main() {
    Box a;
    a.x = 7;
    i32 cond = 1;

    if (cond) {
        consume(a);
    }

    return a.x; // invalid: a may have been moved by the then branch
}
