typedef struct {
    i32 x;
} Box;

void consume(Box b) {
    b.x = b.x;
}

i32 main() {
    Box a;
    a.x = 7;
    consume(a);
    return a.x; // invalid: a is moved into consume(a), so using a afterwards must fail
}
