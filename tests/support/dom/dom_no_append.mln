// Same vocabulary as dom.mln, but append_child is not exported, so nesting a
// child has nothing to lower to.
package dom;

void append_child(i32 p, i32 c) { }

export i32 Window(char *title, i32 x, i32 y, i32 w, i32 h) { return 1; }
export i32 Text(char *text, i32 x, i32 y) { return 3; }
