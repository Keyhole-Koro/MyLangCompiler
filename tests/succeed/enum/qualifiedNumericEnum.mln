enum Color { Red, Green, Blue }
enum Status { Red, Ok }

i32 main() {
    i32 a = Color::Red;
    i32 b = Color::Blue;
    i32 c = Status::Red;
    i32 d = Status::Ok;
    return a * 1000 + b * 100 + c * 10 + d; // 0*1000 + 2*100 + 0*10 + 1 = 201
}
