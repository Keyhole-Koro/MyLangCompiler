// A direct aggregate call can supply a by-value aggregate parameter.
typedef struct { i32 x; i32 y; } P;

P make() { P r; r.x = 1; r.y = 2; return r; }
i32 take(P p) { return p.x + p.y; }

i32 main() {
    return take(make());
}
